- hosts: localhost
  connection: local
  become: true
  pre-tasks:
          - name: update repository
            dnf:
                    update_cache: yes
            when_change: false

  tasks:
          - include: tasks/package.yml
